import { TruncSymbolPipe } from './trunc-symbol.pipe';

describe('TruncSymbolPipe', () => {
  it('create an instance', () => {
    const pipe = new TruncSymbolPipe();
    expect(pipe).toBeTruthy();
  });
});
